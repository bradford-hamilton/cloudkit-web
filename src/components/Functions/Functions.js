import React from 'react';
import { withRouter } from 'react-router-dom';
import './Functions.scss';

const Functions = ({ history }) => {
  return <h1>Functions</h1>;
}

export default withRouter(Functions);